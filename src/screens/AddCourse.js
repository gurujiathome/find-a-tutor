import React, {Component} from 'react';
import {Container, Header, Title, Content, Left, Right, Body, Icon, Button, Text} from 'native-base';

export class AddCourse extends Component {
    render() {
        return (
            <Container>
                <Content padder>
                    <Text>Tu będzie lista kursów</Text>
                </Content>
            </Container>
        );
    }
}